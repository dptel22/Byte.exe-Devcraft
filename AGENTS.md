Antigravity IDE Agent: Handles the high-level system architecture and core Python logic (main.py).

OpenCode Terminal Agent: Only writes isolated Python utility functions, handles data parsing scripts, and writes unit tests.

Core Rule: The agents must never modify the same file concurrently.
